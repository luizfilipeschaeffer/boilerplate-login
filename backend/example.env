# Exemplo de arquivo .env para o backend
# Copie este arquivo para .env e preencha com seus dados reais

# String de conexão do PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/nome_do_banco
DATABASE_URL=postgres://usuario:senha@localhost:5432/boilerplate

# Chave secreta para assinar JWTs (use uma string forte e única)
JWT_SECRET=sua_chave_secreta

# Configurações de e-mail (Nodemailer)

SMTP_HOST=smtp.seuprovedor.com
SMTP_PORT=587
SMTP_USER=seu@email.com
SMTP_PASS=sua_senha
SMTP_FROM="Nome do Projeto <noreply@seudominio.com>" 





