# URL de conexão com o banco de dados PostgreSQL.
# Exemplo: postgresql://user:password@host:port/database
POSTGRES_URL=""

# Segredo para a geração de tokens JWT (JSON Web Tokens).
# Pode ser qualquer string longa e segura. Use `openssl rand -base64 32` para gerar uma.
JWT_SECRET=""

# Chave da API do Resend para envio de e-mails transacionais (verificação, reset de senha, etc).
# Obtenha em: https://resend.com/
RESEND_API_KEY=""

# URL base da sua aplicação em produção.
# Usada para gerar links de verificação e reset de senha nos e-mails.
# Exemplo: https://seu-projeto.vercel.app
NEXT_PUBLIC_BASE_URL=""

# Store ID do seu Vercel Blob para upload de arquivos.
# Obtenha no dashboard do seu projeto na Vercel.
BLOB_READ_WRITE_TOKEN=""
